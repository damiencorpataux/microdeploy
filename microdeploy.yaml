packages:
  example:
    files:
      - example/*.py
    run:
      example/example-test.py

device:
  port: /dev/ttyUSB0  # Note: multiple device definitions may be implemented later
  # baudrate: 115200


# # TODO:
# device:
#   port: /dev/ttyUSB0
#   baudrate:
#     device: 115200
#     flash: 921600
